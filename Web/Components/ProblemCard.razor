@using CodeStudy.Models 
<a class="d-flex flex-column align-items-center text-center rounded text-decoration-none text-dark m-2" href="/problems/@problem.ID">
    <div class="p-2">
        <p class="p-0 m-0 fw-bold">@problem.Name</p>
        <p class="p-0 m-0">
            @foreach (string tag in problem.Tags.Select(x => x.Name))
            {
                <span class="badge bg-primary">@tag</span>
            }
        </p>
        <p class="p-0 m-0">@problem.Article.Username</p>
    </div>
</a>

@code {
    [Parameter]
    public ProblemDTO problem {get;set;}
}
